<div class="watch">
	<div class="col-md-8">
		<h3 class="page-header">{{model.youtube.title}}</h3>
		{{video-frame model=model.youtube myPlayer=myPlayer}}
	</div>
	<div class="col-md-4">
		<div class="row">{{tag-cloud model=model.notes}}</div>
		<div class="row">{{notes-frame model=model myPlayer=myPlayer}}</div>
	</div>
	<div id="tag-cloud" class="col-md-10 col-md-offset-1"></div>
</div>